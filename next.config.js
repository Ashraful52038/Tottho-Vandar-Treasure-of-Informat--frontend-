/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    unoptimized: true,
  },
  // experimental খালি রাখুন
  experimental: {},
};

module.exports = nextConfig;